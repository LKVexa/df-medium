# Persistence Specification
BRCR/1 provides authenticated dual control records, inactive-slot staging, flush/sync/atomic replacement, first-boot TESTING, confirmation, previous-good rollback, and monotonic trust sequencing. BRGD/1 provides separate authenticated guest data. A legacy CRC record can identify an old format but cannot establish trust.
